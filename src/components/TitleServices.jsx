import styled from "styled-components";

const TitleServicesStyle = styled.h1`
  color: var(--color--textPrimary);
  font-size: 2rem;
`;

const TitleServices = () => {
  return <TitleServicesStyle>GUIA DE SERVICIOS 2024</TitleServicesStyle>;
};

export default TitleServices;
